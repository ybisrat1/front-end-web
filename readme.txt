front end web-development project 1:

This project renders a front end webpage using htlm 5 and CSS.
It contains three major container sections (header, Middle (GTA-logo) & end (car images/links).



installion/running:

no installtion required to run other than access to a webbrowser.
server will need:
-HTML file (project1.html)
-CSS file (project.css)
-to have images available to render the page (m3/sky/supra/GTA-logo/logo).